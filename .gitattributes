# Normalise line endings in the repo, check out native on each platform.
* text=auto

# Keep these LF even on Windows — shell scripts break with CRLF.
*.sh text eol=lf

# Binary files Git must not touch.
*.png binary
*.jpg binary
*.pdf binary
*.ico binary
